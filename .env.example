# MongoDB (local) — para produção use MongoDB Atlas ou Supabase
MONGO_URL=mongodb://localhost:27017
DB_NAME=barbershop

# URL pública do app (Netlify preview / produção)
NEXT_PUBLIC_BASE_URL=http://localhost:3000
CORS_ORIGINS=*
